name: CI/CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: pip install -r app/requirements.txt

      - name: Run tests
        run: |
          cd app
          pytest test_app.py

  build-and-push:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          context: ./app
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/flask-app:latest

  deploy:
    runs-on: ubuntu-latest
    needs: build-and-push
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Kubeconfig
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBECONFIG }}" > ~/.kube/config
          chmod 600 ~/.kube/config

      - name: Test cluster connectivity
        run: |
          echo "Testing connection to K3s API server..."
          kubectl cluster-info || echo "WARNING: Cannot reach cluster API server"
          kubectl get nodes --request-timeout=10s || echo "WARNING: Cannot list nodes"
      - name: Debug kubeconfig
        run: |
          echo "=== Taille du fichier ==="
          wc -c ~/.kube/config

          echo "=== Première ligne ==="
          head -1 ~/.kube/config

          echo "=== Est-ce du base64 brut ? ==="
          file ~/.kube/config
      - name: Deploy to K3s cluster
        run: |
          sed -i "s|YOUR_DOCKER_USERNAME|${{ secrets.DOCKERHUB_USERNAME }}|g" k8s/deployment.yaml
          kubectl apply -f k8s/deployment.yaml --validate=false
          kubectl rollout status deployment/flask-app --timeout=120s
